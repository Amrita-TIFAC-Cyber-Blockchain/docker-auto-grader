name: Docker Auto-Grader IPFS

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  grade:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install requests

      - name: Create logs directory
        run: mkdir -p logs

      - name: Run Docker Auto-Grader for IPFS Images
        run: |
          echo "============================="
          echo "🚀 Starting Docker Auto-Grader for IPFS Images"
          echo "============================="
          echo "image,result" > results.csv

          while read -r image; do
            [[ -z "$image" ]] && continue

            echo "--------------------------------------"
            echo "Testing image: $image"
            echo "--------------------------------------"
            log_file="logs/${image//\//_}.log"
            container_name="ipfs_test_$(date +%s)"

            {
              echo "🔹 Pulling image: $image"
              if docker pull "$image"; then
                echo "✅ Image pulled successfully."
              else
                echo "❌ FAILED: Unable to pull $image"
                echo "$image,FAIL" >> results.csv
                exit 0
              fi

              echo "🚀 Starting container: $container_name"
              if ! docker run -d --name "$container_name" -p 4001:4001 -p 5001:5001 -p 8080:8080 "$image"; then
                echo "❌ FAILED: Could not start container."
                echo "$image,FAIL" >> results.csv
                exit 0
              fi

              echo "⏳ Waiting for IPFS daemon to start..."
              sleep 15

              echo "🔍 Running IPFS Python test script..."
              if python3 tests/test_script_ipfs.py "$container_name"; then
                echo "✅ PASS - $image"
                echo "$image,PASS" >> results.csv
              else
                echo "❌ FAIL - $image"
                echo "$image,FAIL" >> results.csv
              fi

              echo "🧹 Cleaning up..."
              docker stop "$container_name" >/dev/null 2>&1 || true
              docker rm "$container_name" >/dev/null 2>&1 || true
              echo ""
            } &> "$log_file"

          done < submissions/ipfs.txt

      - name: Upload grading results and logs
        uses: actions/upload-artifact@v4
        with:
          name: ipfs-grading-results
          path: |
            results.csv
            logs/
